# Dashboard

`frontend/dashboard` is the main consumer-facing app package.

Current scope:

- depends on `@gleaph/sdk`
- demonstrates IC-backed graph access in `src/sdk-example.ts`
- is the intended home for generated prepared clients and authenticated flows
