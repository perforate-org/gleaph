# Ops dashboard (planned)

Clone `apps/dashboard` when internal operator UI is ready. Same stack: Vite, Solid CSR, solid-ui, TanStack Router file-based.

Planned routes: `/`, `/shards`, `/health` (read-only by default).
