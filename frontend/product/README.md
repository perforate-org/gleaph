# Product Web

`frontend/product` is the public-facing product website package.

Current scope:

- holds the product-site workspace boundary
- keeps a minimal TypeScript entrypoint for future site composition
- intentionally does not depend on `@gleaph/sdk` yet
