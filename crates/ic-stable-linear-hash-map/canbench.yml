# Dedicated target so canbench's wasm32 artifact does not replace ordinary workspace outputs.
build_cmd: "CARGO_TARGET_DIR=../../target/canbench cargo build -p ic-stable-linear-hash-map --release --target wasm32-unknown-unknown --features canbench"
wasm_path: "../../target/canbench/wasm32-unknown-unknown/release/ic_stable_linear_hash_map.wasm"
init_args:
  hex: "4449444c0000"
