//! Canister error type — re-exported from the shared kernel so mutation wire returns and admin
//! string mapping use a single source of truth (ADR 0031).

pub use gleaph_graph_kernel::vector_index::VectorIndexError;
