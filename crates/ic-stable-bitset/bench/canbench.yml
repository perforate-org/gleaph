# Paths are relative to this file’s directory (`crates/ic-stable-bitset/bench`).
build_cmd: "cargo build --release --target wasm32-unknown-unknown -p ic-stable-bitset-canbench"
wasm_path: "../../../target/wasm32-unknown-unknown/release/ic_stable_bitset_canbench.wasm"
init_args:
  hex: "4449444c0000"
