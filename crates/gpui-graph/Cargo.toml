[package]
name = "gpui-graph"
description = "A composable property graph visualization component for GPUI"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
gpui = "0.2.2"
slotmap = "1.1"
bitvec = "1.1"
glam = "0.33"

[dev-dependencies]
gpui = { version = "0.2.2", features = ["test-support"] }
