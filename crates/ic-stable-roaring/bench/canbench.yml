# Paths are relative to this file’s directory (`crates/ic-stable-roaring/bench`).
build_cmd: "cargo build --release --target wasm32-unknown-unknown -p ic-stable-roaring-canbench"
wasm_path: "../../../target/wasm32-unknown-unknown/release/ic_stable_roaring_canbench.wasm"
init_args:
  hex: "4449444c0000"

