build_cmd: "CARGO_TARGET_DIR=../../target cargo build --release --target wasm32-unknown-unknown --locked --features canbench"
wasm_path: "../../target/wasm32-unknown-unknown/release/gleaph_graph_catalog.wasm"
init_args:
  hex: "4449444c0000"
