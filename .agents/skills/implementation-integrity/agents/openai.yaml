interface:
  display_name: "Implementation Integrity"
  short_description: "Prevent boundary and contract defects before review"
  default_prompt: "Use $implementation-integrity to implement this slice with invariant-first self-review."
