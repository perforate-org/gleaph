# yaml-language-server: $schema=https://github.com/dfinity/icp-cli/raw/refs/tags/v0.1.0/docs/schemas/icp-yaml-schema.json

canisters:
  - name: registry
    recipe:
      type: "@dfinity/rust"
      configuration:
        package: gleaph-registry
        shrink: true
        compress: true

  - name: graph
    recipe:
      type: "@dfinity/rust"
      configuration:
        package: gleaph-graph
        shrink: true
        compress: true
