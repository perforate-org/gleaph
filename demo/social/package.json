{
  "name": "@gleaph/social-demo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build:gql-formatter": "node scripts/build-gql-formatter.mjs",
    "generate:avatars": "node scripts/generate-avatar-assets.mjs",
    "generate:client": "gleaph codegen",
    "check": "pnpm run build:config && pnpm run build:gql-formatter && tsc --noEmit",
    "build": "pnpm run build:config && pnpm run build:gql-formatter && vite build",
    "build:config": "node scripts/build-config.mjs",
    "dev": "vite",
    "preview": "vite preview"
  },
  "dependencies": {
    "@gleaph/sdk": "workspace:*",
    "@icp-sdk/core": "^6.0.0",
    "@solid-primitives/i18n": "^2.2.1",
    "@solid-primitives/intersection-observer": "2.1.5",
    "solid-js": "^1.9.14"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.2",
    "@types/node": "^26.1.0",
    "autoprefixer": "^10.5.2",
    "tailwindcss": "^4.3.2",
    "typescript": "^7.0.0",
    "vite": "^8.0.0",
    "vite-plugin-solid": "^2.11.12",
    "yaml": "^2.9.0"
  }
}
